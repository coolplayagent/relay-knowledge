interface:
  display_name: "Relay Knowledge CLI"
  short_description: "Commit-driven repo graphs and bounded indexing"
  default_prompt: "Use $relay-knowledge-cli to update the repository through durable commit tasks, pin the response's immutable task or summary base/head, wait for the exact resolved head, run impact and context without replaying update, inspect software or OKF graphs when relevant, and verify retention diagnostics."
