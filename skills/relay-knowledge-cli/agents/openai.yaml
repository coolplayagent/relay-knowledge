interface:
  display_name: "Relay Knowledge CLI"
  short_description: "Query code definitions, calls, and knowledge graphs"
  default_prompt: "Use $relay-knowledge-cli first for code maps, function definitions, references, callers, callees, call chains, repository indexing, and code graph questions."
