interface:
  display_name: "Relay Knowledge CLI"
  short_description: "Query repo graph kinds by CLI"
  default_prompt: "Use $relay-knowledge-cli first for repo query --kind and repo software --kind graph requests, including relationships, before grep/ripgrep/rg fallback."
