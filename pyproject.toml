[project]
name = "relay-knowledge-test-harness"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = [
  "playwright>=1.57.0",
  "pytest>=9.0.0",
  "pytest-playwright>=0.7.0"
]

[tool.pytest.ini_options]
testpaths = ["tests/browser"]
